This directory contains some unit tests for Ray.

# To run unit tests

bash main.sh

# handy tools 
To test the coverage distribution:

g++ coverage_main.cpp ../code/graph/CoverageDistribution.cpp -O3 -o CoverageExe -I ../code -D DEBUG_CoverageDistribution -DASSERT
