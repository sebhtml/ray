* run regression tests (+backup on colosse)
* run unit tests
* update ChangedLog (based on git log)
* set ASSERT to no.
* set MAXKMERLENGTH to 32
* check version in constants.h + manual
* test compilation on a git clone __on 5 different computers__ (make + make install): 
 - ls30
 - iceman (colosse2)
 - cortex
 - globe,
 - lw132
 - Visual Studio (VirtualBox)
* git tag vx.y.z
* close the milestone in trac
* git checkout tag,
* compile the manual
* do a tar.bz2, compile it (make + make install) and test it
* upload the tarball on sf
* update the web site (front page)
* post an email on the list including changes (ChangeLog) 
* update the seqanswer thread
