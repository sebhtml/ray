* use scripts/run-all.sh to generate results
* update NEWS (based on svn log and trac milestone)
* svn cp trunk tags/<version>
* close the milestone in trac
* svn export
* do a tar.bz2, compile it to test it
* upload the tarball on sf
* update the wiki page
* post an email on the list including changes and results on benchmarks
* update the seqanswer thread
